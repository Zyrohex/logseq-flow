// Setup our static elements
let newElement = document.createElement("div").classList.add("tagRefPanel")
let newContent = document.createElement("div").classList.add("content")
let titleContent = document.createElement("div").classList.add("foldable-title")

// Create our event handler
function buildTagPanel(elements) {
    // create our panel

    // add our title and collapse functions

    // process our tagged elements and move them to our new panel
}

// Create our event listener